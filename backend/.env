# Keep your existing DB settings for local testing
POSTGRES_DB=budgetly_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Student@945
DB_HOST=localhost
DB_PORT=5432

# ADD YOUR LIVE URLS HERE (Comma separated)
# 1. Add your GitHub Pages URL so React can talk to Django
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://cs571-s26.github.io/p159/

# 2. Add your Render URL so Django knows it's allowed to run there
ALLOWED_HOSTS=127.0.0.1,localhost,budgetly-backend-4y3s.onrender.com